name: Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
            nuitka-args: --windows-console-mode=attach --include-package=flet
            artifact-dir: dist/main.dist

          - os: macos-latest
            label: macOS
            nuitka-args: --macos-create-app-bundle --include-package=flet
            artifact-dir: dist/main.app

          - os: ubuntu-latest
            label: Linux
            nuitka-args: --include-package=flet
            artifact-dir: dist/main.dist

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc python3-dev tk-dev libx11-dev

      - name: Install system deps (macOS)
        if: runner.os == 'macOS'
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
        run: |
          brew install python-tk@3.12 || true

      - name: Install dependencies
        run: |
          pip install httpx flet playsound3 python-dotenv nuitka

      - name: Build with Nuitka
        shell: bash
        run: |
          python -m nuitka --standalone \
            --enable-plugin=tk-inter \
            --include-package=gui,httpx,playsound3,dotenv \
            --include-package-data=flet \
            --include-data-dir=gui=gui \
            --output-dir=dist \
            --output-filename=ATRI-IndexTTS \
            ${{ matrix.nuitka-args }} \
            --assume-yes-for-downloads \
            main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ATRI-IndexTTS-${{ matrix.label }}
          path: ${{ matrix.artifact-dir }}
